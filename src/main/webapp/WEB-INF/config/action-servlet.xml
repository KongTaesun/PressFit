<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
    xmlns:p="http://www.springframework.org/schema/p"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.3.xsd">

    <!-- DispatcherServlet Context: defines this servlet's request-processing 
        infrastructure -->
 
    <!-- Enables the Spring MVC @Controller programming model -->
    <annotation-driven />
 
    <!-- Handles HTTP GET requests for /resources/** by efficiently serving 
        up static resources in the ${webappRoot}/resources directory -->
    <resources mapping="/resources/**" location="/resources/" />
    <resources mapping="/WEB-INF/ckediter/**" location="/WEB-INF/ckediter/" />
    <resources mapping="/WEB-INF/include/**" location="/WEB-INF/include/" />
 
    <!-- Resolves views selected for rendering by @Controllers to .jsp resources 
        in the /WEB-INF/views directory -->
    <beans:bean class="org.springframework.web.servlet.view.BeanNameViewResolver" p:order="0" />
    <beans:bean id="jsonView" class="org.springframework.web.servlet.view.json.MappingJackson2JsonView" />

    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/WEB-INF/jsp/" />
        <beans:property name="suffix" value=".jsp" />
    </beans:bean>
  
    <!-- 스프링 빈을 태그로 등록하지 않고 자동으로 검색(auto scan) --> 
    <context:component-scan base-package="kr.co.pressfit" />

	<beans:bean id="sampleInterceptor" class= "kr.co.pressfit.interceptor.SampleInterceptor"/> 
	<beans:bean id="loginInterceptor" class= "kr.co.pressfit.interceptor.LoginInterceptor"/>
	<beans:bean id="adminInterceptor" class= "kr.co.pressfit.interceptor.AdminInterceptor"/>
	<beans:bean id="adminclearInterceptor" class= "kr.co.pressfit.interceptor.AdminclearInterceptor"/>
	<interceptors> 
		<interceptor> 
				<mapping path="/admin/*.do"/>
				<beans:ref bean="adminInterceptor" />
		</interceptor>
		<interceptor> 
				<mapping path="/tmouse/*.do"/>
				<mapping path="/keyboard/*.do"/>
				<mapping path="/gallery/*.do"/>
				<mapping path="/business/*.do"/>
				<mapping path="/shop/cart/*.do"/>
				<mapping path="/community/*.do"/>
				<mapping path="/faq/*.do"/>
				<mapping path="/member/*.do"/>
				<beans:ref bean="adminclearInterceptor" />
		</interceptor>
	</interceptors>


<!-- 
	<interceptors>
	
관리자 세션 체크를 위한 인터셉터 설정	
		 
		<interceptor>
			<mapping path="/tmouse/write.do"/>
			<mapping path="/tmouse/insert.do"/>
			<mapping path="/keyboard/write.do"/>
			<mapping path="/keyboard/insert.do"/>
			<beans:ref bean="adminInterceptor" />
		</interceptor> 
		<interceptor> 
		
			<mapping path="/gallery/write.do"/>
			<mapping path="/gallery/insert.do"/> 
			<mapping path="/gallery/update.do"/>
			<mapping path="/gallery/delete.do"/>
			<mapping path="/gallery/view.do"/>
			
			<mapping path="/shop/cart/insert.do"/>
			<mapping path="/shop/cart/list.do"/> 
			<beans:ref bean="loginInterceptor" />
		</interceptor>
		
	</interceptors>  

 -->
 </beans:beans>

